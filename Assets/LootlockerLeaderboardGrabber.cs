using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using LootLocker.Requests;

public class LootlockerLeaderboardGrabber : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        //GetAllMemberRanks();
    }

    //public void GetAllMemberRanks(Action<LootLockerGetAllMemberRanksResponse>);

    // Update is called once per frame
    void Update()
    {
        
    }
}
